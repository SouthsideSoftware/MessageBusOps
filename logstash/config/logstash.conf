input {
	tcp {
		port => 5000
	}
	rabbitmq {
        host => "rabbit"
        durable => true
        user => "test"
        password => "test"
        queue => "logstash-audit"
        port => 5672
        vhost => test
    }
}

## Add your filters here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
	}
	stdout { codec => rubydebug }
}
